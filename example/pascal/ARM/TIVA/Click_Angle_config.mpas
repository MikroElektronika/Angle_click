unit Click_Angle_config;

uses Click_Angle_types;

const
    _ANGLE_I2C_CFG : array[1] of uint32_t = (_I2C_BITRATE_STANDARD_MODE); 
    _ANGLE_UART_CFG : array[6] of uint32_t = (9600, 0, _UART_8_BIT_DATA, _UART_NOPARITY, _UART_ONE_STOPBIT, _UART_HIGH_SPEED); 
    
end.